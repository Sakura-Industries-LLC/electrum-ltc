# Runtime dependencies of the DNTLS Python SDK (dntls-testnet-sdk), which the
# "dntls" plugin imports. The SDK itself is not on PyPI: the build installs it
# from the private dntls-testnet repository at the commit in
# contrib/dntls/sdk-commit.txt, with --no-deps, so its dependencies are pinned
# here instead. Keep this list in sync with sdk/python/pyproject.toml.
#
# Generated into contrib/deterministic-build/requirements-dntls.txt; see the
# header of that file.
argon2-cffi>=25.1.0
cbor2>=6.1.4
cryptography>=50.0.1
httpcore>=1.0.9
httpx>=0.28.1
jsonschema>=4.26.0
pynacl>=1.6.2
pyopenssl>=26.4.0
